之前工程里面忽略了一个文件，发现其他人拉去了之后，本地都消失了，这个超出我的认知，我需要测试一下

这句话应该不会被上传吧
	单纯把一个文件添加到 .ignore中，该文件还是可以 pull/push的，只有把它设置为删除（远端仓库删除），才能真正删除，但是这样就导致了其他人也删除了 

我现在通过 git update-index [--[no-]assume-unchanged]，再试试
	git update-index [--[no-]assume-unchanged]，也不行，因为它可以防止本地更改不会上传，远程仓库也没有删除；但是其他node还是可以上传这个文件


我现在试试 git rm --cached
	这个命令也不行，虽然本地不删除；但是远端仓库会删除



我试试另一个命令 git update-index [--[no-]skip-worktree] 
	这个命令跟 assume-unchanged 很像

测试 skip-worktree 和 assume-unchanged 区别
	skip-worktree: 即便我本地设置了，从远端还能拉取到变化
	assume-unchanged：也能从远端拉取到变化